# Personal Site

[Live Site](https://www.kushagrabharti.com)

This repository powers two connected products in one codebase:

- `portfolio`: the public portfolio site, public API, `/ai`, and generated `llms.txt`
- `tracker`: a private task, workflow, and calendar-connected planning app

The backend owns portfolio content and live integrations. The frontend owns rendering, interaction, routing, and the private tracker experience.

![Personal Site Preview](personal-site-preview.png)

## Product Surfaces

### Portfolio

The public site lives at `/`. It statically prerenders the real React homepage shell at build time, hydrates that same shell in the browser, then loads media-heavy enhancements, the 3D hero model, and live data after the first paint without skeleton placeholders.

Machine-readable portfolio surfaces:

- `/ai` renders a structured AI-readable portfolio page.
- `/llms.txt` is generated from the backend portfolio snapshot.
- `frontend/index.html`, `frontend/ai.html`, `frontend/public/llms.txt`, `frontend/public/robots.txt`, `frontend/public/sitemap.xml`, `frontend/src/portfolio/generated/introBootstrap.ts`, and `frontend/src/portfolio/generated/portfolioSnapshotBootstrap.ts` are generated by `frontend/scripts/sync-portfolio-exports.mjs`.

Portfolio content starts in `backend/src/portfolio/content`. The frontend should render that data instead of becoming a second content source. Public portfolio fonts are self-hosted under `frontend/public/portfolio/fonts` and should not be reintroduced through Google Fonts.

### Tracker

The private tracker lives at `/tracker`. It is lazy-loaded, Supabase-authenticated, and split into modules:

- `tasks`: the current tasks-hub interface
- `weekly`: the older weekly tasks module
- `pipeline`: active deals/workflow tracking

Most tracker CRUD talks directly to Supabase from the frontend. Backend private APIs handle service-role operations, task-list deletion, cron, and Google Calendar sync.

## Repository Layout

```text
frontend/
  src/portfolio/   public portfolio UI, API clients, sections, pages
  src/tracker/     private tracker shell, modules, auth, Supabase client
  src/shared/      shared frontend components and utilities
  scripts/         portfolio export sync

backend/
  src/portfolio/   portfolio content, contracts, routes, controllers, services
  src/tracker/     tracker calendar, cron, task-list services
  src/routes/      root API mounting

scripts/
  verify.mjs       repo-level verification runner
```

## Routes

Frontend:

- `/` -> public homepage
- `/ai` -> AI-readable portfolio page
- `/tracker` -> private tracker

Backend:

- `/api/portfolio`
- `/api/portfolio/llms.txt`
- `/api/intro`
- `/api/projects`
- `/api/experiences`
- `/api/education`
- `/api/github/stats`
- `/api/weather`
- `/api/private/calendar`
- `/api/private/cron`
- `/api/private/lists`

## Tech Stack

- Frontend: React, TypeScript, Vite, Tailwind CSS, Framer Motion, Three.js
- Backend: Express, TypeScript, Axios
- Data/Auth: Supabase
- Integrations: GitHub API, OpenWeather, Google Calendar
- Testing: Vitest, Testing Library, Supertest, Playwright
- Deployment/Tooling: Vercel, Bun

## Getting Started

### Prerequisites

- Bun
- GitHub token for the public GitHub stats API
- OpenWeather API key for the public weather API
- Supabase credentials for tracker auth/data
- Google Calendar credentials if working on calendar sync

### Frontend

```bash
cd frontend
bun install
cp .env.local.example .env.local
bun run dev
```

Common frontend env:

```bash
VITE_API_BASE_URL=http://localhost:5000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
```

### Backend

```bash
cd backend
bun install
cp .env.example .env
bun run dev
```

Key backend env:

```bash
GITHUB_USERNAME=
GITHUB_TOKEN=
OPENWEATHER_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
```

## Common Commands

Frontend:

```bash
cd frontend
bun run dev
bun run build
bun run lint
bun run test:unit
bun run test:integration
```

Backend:

```bash
cd backend
bun run dev
bun run build
bun run lint
bun run test:unit
bun run test:integration
```

Repo-level verification:

```bash
bun install
bun run verify
bun run verify:full
```

Verification tiers:

- `bun run verify` -> npm-backed installs, then build, lint, unit, integration
- `bun run verify:live` -> `verify` plus live backend checks
- `bun run verify:full` -> `verify` plus Playwright smoke and mocked E2E
- `bun run verify:full:live` -> everything

The repo is Bun-first for normal app work. The root `verify` scripts intentionally use npm internally because the test toolchain has been more reliable that way in the Windows + OneDrive environment.

## Development Notes

- Preserve the portfolio/tracker separation in both frontend and backend.
- Keep public portfolio asset paths rooted at `/portfolio/...`.
- Use slug-based, ordered portfolio records.
- Remove placeholder links instead of leaving `#`.
- Weather must stay backend-driven and must not trigger browser location prompts.
- GitHub stats should prefer the GraphQL contribution path.
- Finance has been removed from the tracker. Do not reintroduce finance modules, routes, or docs.
- `AGENTS.md`, `CLAUDE.md`, and this `README.md` are the only root docs intended to remain.
